{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn wsrun --exclude-missing build",
    "watch": "yarn wsrun --exclude-missing watch",
    "update-tarball": "cd packages/inertia-react && npm pack --pack-destination=../../"
  },
  "dependencies": {
    "wsrun": "^5.2.1"
  }
}
